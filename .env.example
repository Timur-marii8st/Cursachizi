# ============================================================
# CourseForge Environment Configuration
# Copy this file to .env and fill in your values
# ============================================================

# --- Application ---
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# --- Database ---
DATABASE_URL=postgresql+asyncpg://courseforge:courseforge@localhost:5433/courseforge

# --- Redis ---
REDIS_URL=redis://localhost:6379/0

# --- LLM Providers ---
# OpenRouter is the recommended provider (single API for Claude/GPT/Gemini)
OPENROUTER_API_KEY=
# Legacy direct provider keys (optional, used as fallback)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Default LLM provider: "openrouter", "anthropic", or "openai"
DEFAULT_LLM_PROVIDER=openrouter
# Default model for content writing
DEFAULT_WRITER_MODEL=google/google/gemini-3.1-flash-lite-preview
# Default model for lightweight tasks (claim extraction, etc.)
DEFAULT_LIGHT_MODEL=stepfun/step-3.5-flash
# Vision model for visual template matching
VISION_MODEL=google/google/gemini-3.1-flash-lite-preview

# --- Search Providers ---
# Tavily API key (primary search)
TAVILY_API_KEY=
# Serper API key (fallback search)
SERPER_API_KEY=

# --- Telegram Bot ---
TELEGRAM_BOT_TOKEN=
# Webhook URL (leave empty for polling in dev)
TELEGRAM_WEBHOOK_URL=

# --- Object Storage (MinIO / S3) ---
S3_ENDPOINT_URL=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET_NAME=courseforge-documents
S3_REGION=us-east-1

# --- API Server ---
API_HOST=0.0.0.0
API_PORT=8000
# IMPORTANT: set to your public server URL in production (e.g. https://yourserver.com)
# Used to generate document download links — must be accessible from outside Docker.
API_BASE_URL=http://localhost:8000
INTERNAL_API_KEY=

# --- Admin ---
# Comma-separated Telegram IDs with unlimited credits (no credit deduction)
ADMIN_TELEGRAM_IDS=

# --- Rate Limiting ---
RATE_LIMIT_PER_USER=10/hour
# Comma-separated IPs of trusted reverse proxies (nginx, etc.)
# X-Forwarded-For is only trusted when the direct connection comes from these IPs.
# Example: TRUSTED_PROXY_IPS=10.0.0.1,172.16.0.1
TRUSTED_PROXY_IPS=

# --- Pipeline Defaults ---
MAX_SEARCH_RESULTS=20
MAX_SOURCES_PER_TOPIC=15
MAX_TOKENS_PER_SECTION=4000
PIPELINE_TIMEOUT_SECONDS=900

# --- Visual Template Matching ---
VISUAL_MATCH_ENABLED=true
VISUAL_MATCH_MAX_ITERATIONS=3

# --- Iterative Fact-Checking ---
FACT_CHECK_MAX_ROUNDS=2

# --- Translation API (for humanizer) ---
# Google Translate API key OR DeepL API key (optional, enables humanizer)
GOOGLE_TRANSLATE_API_KEY=
DEEPL_API_KEY=

# --- Robokassa Payment ---
ROBOKASSA_LOGIN=
ROBOKASSA_PASSWORD1=
ROBOKASSA_PASSWORD2=
ROBOKASSA_TEST_MODE=true

# --- Docker Service Credentials (override in production) ---
POSTGRES_PASSWORD=courseforge
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
